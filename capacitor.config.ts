import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-dev-integrative',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
